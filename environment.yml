name: md2docx
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi==0.115.5
      - uvicorn[standard]==0.32.1
      - python-multipart==0.0.12   # necesario para form data en FastAPI
      - python-docx==1.1.2
      - mistune==3.0.2
